name: secret_key

on:
    push:
        branches: [main]
jobs:
    build-and-deploy:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Use API Key
              run: |
                    echo "Using API Key: ${{ secrets.API_KEY }}"


